#include "Function.h"
#include <iostream>
#include <cmath>
using namespace std;

double Function::squareAreaCircle()
{
    double area;
    
    area = pow(circleArea(0), 2);
    
    return area;
}